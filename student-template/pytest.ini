[pytest]
# Одинаковое имя test_example.py в API и UI не должно ломать сборку тестов.
addopts = --import-mode=importlib
testpaths = tests
pythonpath = .
markers =
    requirement(id): требование из задания
    api: проверка REST API
    ui: проверка интерфейса
