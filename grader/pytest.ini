[pytest]
# API/UI могут содержать одноимённые модули: импортируем по полному пути, не общему basename.
addopts = --import-mode=importlib
markers =
    requirement(id): требование задания
    defect(id): контроль дефекта
    api: API-тест
    ui: UI-тест
