[pytest]
testpaths = quality/unit
pythonpath = .
addopts = -ra
markers =
    requirement(id): идентификатор проверяемого требования
    defect(id): контрольный тест учебного дефекта
    api: проверка REST API
    ui: проверка интерфейса
    sql: проверка PostgreSQL
